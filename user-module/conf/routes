# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /v1/test                      @controllers.UserController.test
POST        /v1/addUser                   @controllers.UserController.addUser
GET         /v1/getUser/:userEmail        @controllers.UserController.getUser(userEmail)

# Swagger
GET         /v1/swagger.json              @controllers.ApiHelpController.getResources
# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                 controllers.Assets.at(path="/public", file)
